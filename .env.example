# Path to the AzerothCore project root (where docker-compose.yml lives)
AC_PROJECT_ROOT=/path/to/azerothcore-wotlk

# SOAP connection settings (worldserver)
SOAP_HOST=127.0.0.1
SOAP_PORT=7878
SOAP_USER=soap
SOAP_PASS=soap

# MySQL connection (ac-database container, exposed on host)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASS=password
